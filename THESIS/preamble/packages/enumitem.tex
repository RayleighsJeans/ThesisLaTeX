\usepackage{enumitem}

% Typeset Descriptions in the following way:
% Definition 1  My text which wraps here
%     to continue in the next line
%     to continue in the next line
%     to continue in the next line
\setlist[description]{leftmargin=*,style=sameline}

% Examples: 
% Labeling M1 ... M2
% \begin{enumerate}[label=\textbf{M.\arabic*}]
%     \item \label{enum:symmetry} some entry
%     \item \label{enum:area} another item
% \end{enumerate}
% 
% We refer to~\ref{enum:symmetry} and \ref{enum:area}.
