% allows easy use of quotes
\usepackage
		[
			strict=true, % report warnings as errors
			autostyle=true, % looks for the babel/polyglossia language and uses that
			german=guillemets, % use german guillemets
		]
		{csquotes}

\setquotestyle{german} % use nice guillemets

%%% example code
% --------
% \enquote{foo \enquote{bar}}
% --------
