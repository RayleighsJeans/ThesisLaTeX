%%% The float package provides us with the H option. This is a stronger version of h, and tells LATEX that we would like the figure to appear exactly here. If we use the H option, but forget to include \usepackage{float} in our preamble, we will get an error like the one shown below
\usepackage{float}