% fix listings and hyperref packages (some other packages like 
% float as well, but float should not be part of the packages as 
% it tends to move people towards using floating environments 
% as nonfloating environments)
\usepackage{scrhack}
