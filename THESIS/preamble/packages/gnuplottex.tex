% write gnuplotcode directly in the editor
\usepackage
		[
			miktex, % use this if you are using Windows
			siunitx, % put the axis labeling in \num{...}. Requires siunitx
% 			subfolder, % automatically put all related files in a subfolder
		]
		{gnuplottex}
