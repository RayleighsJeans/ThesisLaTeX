% easy use of quotes
\usepackage{epigraph}

%%% settings
% \setlength{\epigraphrule}{0pt} % thickness of separating rule, use 0pt to draw no rule at all
\renewcommand{\textflush}{flushepinormal} % necessary if you make the quote italic

%%% example code
% \setlength{\epigraphwidth}{6cm} % width of quote
% \epigraph{quote}{source}
